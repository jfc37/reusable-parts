import { Component, ChangeDetectionStrategy } from '@angular/core';

@Component({
  selector: 'vallum-callback',
  template: ``,
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class CallbackComponent {}
