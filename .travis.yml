language: node_js
node_js:
  - "8"
dist: trusty
sudo: false

branches:
  only:
    - feature/travis-ci

cache:
  directories:
    - node_modules

addons:
  chrome: stable

script:
  - ng build --app=sous-chef
