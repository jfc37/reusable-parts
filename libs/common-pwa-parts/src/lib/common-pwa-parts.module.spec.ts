import { CommonPwaPartsModule } from './common-pwa-parts.module';

describe('CommonPwaPartsModule', () => {
  it('should work', () => {
    expect(new CommonPwaPartsModule()).toBeDefined();
  });
});
