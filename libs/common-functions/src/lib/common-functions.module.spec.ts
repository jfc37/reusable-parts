import { CommonFunctionsModule } from './common-functions.module';

describe('CommonFunctionsModule', () => {
  it('should work', () => {
    expect(new CommonFunctionsModule()).toBeDefined();
  });
});
